# Copy to .env and fill in your values
ANTHROPIC_API_KEY=sk-ant-...

# Only for remote mode (Cowork / cloud chat via serve_remote.py).
# Leave this out — serve_remote.py generates and saves one automatically.
# It becomes the secret path that protects your tunnelled server.
# HIVEMIND_TOKEN=
